# Runtime dependencies for installing the SDK from source.
# Keep in sync with `dependencies` in pyproject.toml; collision detection
# dependencies are part of the core install.
# Development and test tools are declared in pyproject.toml under the dev extra.
pysoem>=1.0.2,<2.0.0
netifaces>=0.11.0
numpy>=1.24.0
pymodbus>=3.0.0,<4.0.0
pyserial>=3.5
numpy-stl>=3.1.0
pandas>=2.0.0
