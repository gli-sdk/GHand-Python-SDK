环境设置
==============

安装Python
----------
Xiaoyao SDK需要Python 3.8或更高版本。

.. code-block:: bash

   # 检查Python版本
   python --version

   # 如果版本低于3.8，请升级Python

安装依赖
--------
使用pip安装所需依赖：

.. code-block:: bash

   pip install -r requirements.txt

配置开发环境
------------
推荐使用VS Code作为开发环境，并安装Python扩展。